<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 教师Mapper：对应 teacher 表的 SQL 操作 -->
<mapper namespace="com.thirteenash.backend.mapper.TeacherMapper">

    <!-- 分页查询教师列表，支持按工号/姓名模糊查询和性别筛选 -->
    <select id="list" resultType="com.thirteenash.backend.entity.Teacher">
        select teacher_id, teacher_no, teacher_name, gender
        from teacher
        <where>
            <if test="keyword != null and keyword != ''">
                and (
                    teacher_no like concat('%', #{keyword}, '%')
                    or teacher_name like concat('%', #{keyword}, '%')
                )
            </if>
            <if test="gender != null and gender != ''">
                and gender = #{gender}
            </if>
        </where>
        order by teacher_id
        limit #{pageSize} offset #{offset}
    </select>

    <!-- 查询教师总数 -->
    <select id="count" resultType="long">
        select count(*)
        from teacher
        <where>
            <if test="keyword != null and keyword != ''">
                and (
                    teacher_no like concat('%', #{keyword}, '%')
                    or teacher_name like concat('%', #{keyword}, '%')
                )
            </if>
            <if test="gender != null and gender != ''">
                and gender = #{gender}
            </if>
        </where>
    </select>

    <!-- 根据ID查询单个教师 -->
    <select id="getById" resultType="com.thirteenash.backend.entity.Teacher">
        select teacher_id, teacher_no, teacher_name, gender
        from teacher
        where teacher_id = #{teacherId}
    </select>

    <!-- 按工号查询数量（排除自身ID，用于唯一性校验） -->
    <select id="countByTeacherNo" resultType="int">
        select count(*)
        from teacher
        where teacher_no = #{teacherNo}
        <if test="teacherId != null">
            and teacher_id != #{teacherId}
        </if>
    </select>

    <!-- 查询教师任课关系数量 -->
    <select id="countClassCourseByTeacherId" resultType="int">
        select count(*)
        from class_course_teacher
        where teacher_id = #{teacherId}
    </select>

    <!-- 新增教师，返回自增主键 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="teacherId">
        insert into teacher (teacher_no, teacher_name, gender)
        values (#{teacherNo}, #{teacherName}, #{gender})
    </insert>

    <!-- 修改教师信息 -->
    <update id="update">
        update teacher
        set teacher_no = #{teacherNo},
            teacher_name = #{teacherName},
            gender = #{gender}
        where teacher_id = #{teacherId}
    </update>

    <!-- 根据ID删除教师 -->
    <delete id="deleteById">
        delete from teacher
        where teacher_id = #{teacherId}
    </delete>

</mapper>
